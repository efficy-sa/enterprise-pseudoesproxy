import { type Config } from 'prettier';

const config: Config = {
    bracketSpacing: true,
    printWidth: 120,
    semi: true,
    singleQuote: true,
    tabWidth: 2,
    trailingComma: 'none'
};

export default config;
